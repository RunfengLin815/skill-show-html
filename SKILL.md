---
name: show-html
description: 将需求确认、架构决策、方案评审、设计 review、调研报告等需要用户审阅的结构化内容生成为带交互评论功能的 HTML 页面，在浏览器中展示供用户逐节审阅和批注
argument-hint: "<topic>"
level: 2
---

## 触发场景

当对话中产出需要用户审阅、确认或决策的结构化内容时使用，包括但不限于：
- 需求边界确认、PRD 审阅
- 架构选型、技术方案评审
- 设计 review（API 设计、数据模型、流程图）
- 上线计划、变更清单
- 调研报告、竞品分析
- 多方案对比与推荐

## 输出规范

### 文件命名与存放
1. 存放目录：当前工作目录下的 `.cc-reviews/`（不存在时自动创建）
2. 文件名格式：`{YYYY-MM-DD-HHmmss}-{topic-slug}.html`，topic-slug 为 2-4 个英文单词的 kebab-case 摘要
3. 示例：`.cc-reviews/2026-05-13-143022-arch-decision.html`

写入后执行 `open <文件路径>` 在浏览器中打开。

### HTML 质量要求
- **自包含**：CSS 内联，不依赖外部资源（CDN、字体、图片链接），确保离线可用
- **排版舒适**：合理的字号（正文 16px+）、行高（1.6+）、段间距，最大阅读宽度 800-900px 居中
- **视觉层次**：标题、正文、代码块、表格、引用块等有明确的视觉区分
- **代码高亮**：内联简易语法高亮（至少区分关键字、字符串、注释），使用等宽字体
- **表格友好**：表格有斑马纹、边框、表头加粗，宽表格支持横向滚动
- **响应式**：在 1024px-1920px 宽度范围内布局合理
- **打印友好**：`@media print` 隐藏交互元素，保留内容

### 内容结构
- 页面顶部包含标题、生成时间、摘要/背景说明
- 内容按逻辑分为多个 section，每个 section 有清晰的标题（h2/h3）
- 如有多方案对比，使用表格或卡片式并排布局
- 页面底部包含评论汇总区和操作按钮

## 评论功能（强制要求）

**每一个 section 都必须提供评论能力**，这是本 skill 的核心交互，不可省略。用户需要在审阅时直接在对应段落记录意见，而非凭记忆回到会话反馈。

### 必须实现的功能

#### 1. 逐节评论入口
- 每个 section（h2/h3 级别的内容块）末尾放置「💬 添加评论」按钮
- 点击按钮展开评论输入区（textarea + 操作按钮），再次点击可折叠
- 按钮上显示该 section 已有评论数量的角标（如 `💬 添加评论 (3)`）

#### 2. 评论的 CRUD
- **创建**：用户输入评论文本后点击「保存」，评论附带时间戳显示在该 section 下方
- **查看**：已保存的评论以列表形式展示，包含评论内容和时间
- **编辑**：每条评论旁有「编辑」按钮，点击后内容回填到 textarea，保存后更新
- **删除**：每条评论旁有「删除」按钮，点击后确认删除（`confirm` 弹窗即可）

#### 3. Section 级别态度标记
- 每个 section 提供快捷态度按钮：「✅ 同意」「⚠️ 有疑问」「❌ 反对」
- 点击后高亮对应按钮，section 标题旁显示对应图标
- 态度标记可切换或取消

#### 4. 数据持久化
- 所有评论和态度标记使用 `localStorage` 持久化
- key 基于文件名 + section 标识，避免跨页面冲突
- 页面刷新后自动恢复所有评论和标记状态

#### 5. 评论导出（页面底部）
- 提供「📋 导出所有评论」按钮
- 导出格式为纯文本，结构清晰，方便直接粘贴到 Claude Code 会话：
  ```
  ## 审阅意见

  ### Section 标题 [✅ 同意 / ⚠️ 有疑问 / ❌ 反对]
  - 评论 1 内容 (2026-05-15 14:30)
  - 评论 2 内容 (2026-05-15 14:35)

  ### 另一个 Section 标题 [未标记]
  （无评论）
  ```
- 点击后自动复制到剪贴板，并显示「已复制」提示
- 未评论的 section 也在导出中列出（标注"无评论"），让用户确认是否遗漏

#### 6. 全局状态概览
- 页面顶部或侧边栏显示审阅进度：`已评论 3/7 个 section`
- 用不同颜色区分已评论/未评论的 section（如目录导航中标记）

### 样式要求
- 评论区域的样式必须与页面整体风格一致，不能看起来像是临时拼凑的
- 评论输入框有合适的尺寸（至少 3 行高度）和 placeholder 提示
- 已保存的评论有轻微的背景色区分，视觉上与正文内容分层
- 态度按钮在未选中时为浅色/描边样式，选中后填充对应颜色

## 用户工作流

1. Claude 生成 HTML 并在浏览器中打开
2. 用户逐节阅读，对每个 section 标记态度、写下评论
3. 审阅完毕后点击「导出所有评论」，内容自动复制到剪贴板
4. 用户回到 Claude Code 会话，粘贴评论汇总，继续讨论
